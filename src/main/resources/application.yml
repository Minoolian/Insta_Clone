spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    password: test
    url: "jdbc:mariadb://15.165.248.169:3306/test?characterEncoding=UTF-8&serverTimezone=UTC"
    username: test
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MariaDBDialect
      format-sql: true
      show-sql: true
      use-sql-comment: true
      ddl-auto: update
    open-in-view: false
    show-sql: true
    generate-ddl: true

  # multipart 설정
  servlet:
    multipart:
      enabled: true
      max-file-size: "2MB"

# 파일설정
profileImg:
  path: "C:/testtemp/profile/"

post:
  path: "C:/testtemp/post/"
  
# 인코딩 설정
#server:
#  servlet:
#    encoding:
#      charset: UTF-8
#      enabled: true
#      force: true



